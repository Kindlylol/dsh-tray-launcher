# v1.0.7-beta.1 验证报告

日期：2026-09-23。范围：Windows x64，本地测试版，不覆盖 v1.0.6，不发布 GitHub。

## 已验证

- Release 构建：零警告、零错误。
- 原有 30 项契约检查：版本渠道、模式保存、API 健康判定、核心 profile 隔离。
- 新增 20 项修复检查：明确诊断与未知原因区分、单项停用、保留依赖、原文件备份、旧快照拒绝、补丁变化拒绝、本工具写锁、重新启用及顺序恢复、补丁/lockfile 不改、日志脱敏、无效清单/停用记录拒绝。
- 真实 DSH 0.1.6-alpha.2 + 隔离 home + 3187 端口的 8 项集成断言：故障插件 import 抛错→核心恢复；停用故障 bundle→插件 RPC 健康；正常夹具插件写入启动标记；部分插件状态；重新启用故障插件→再次核心恢复；没有静默修改用户启用选择。测试结束停止自己的服务。
- 原生 Windows 界面：实际查看窗口；选中、停用确认、备份结果、列表状态变化、重新启用及恢复顺序；最终布局包含“启动 Beta 插件环境”。UI 使用隔离清单及模拟启动回调；真实启动由上述集成测试单独覆盖。
- 两种最终安装包各运行契约和修复检查；ZIP 完整性、文档一致性、校验文件另行核验。

## 未验证与边界

- 没有把用户整套真实插件导入 Beta；不声称生态兼容，也未执行真实核心版本升级。
- 没有在缺少 .NET 的干净 Windows 上验证安装弹窗与下载按钮。
- 不实现任意堆栈自动归因、依赖闭包自动停用、任意 YAML 修复、第三方插件沙箱或自动包升级。
- 修复事务在停掉 Beta 服务后执行，包含快照指纹及同工具锁；非协作的外部编辑器仍可能在最后指纹检查和原子替换之间竞争。测试时不要并发执行包管理或编辑配置。
- 备份保留原清单全部字节，可能包含本地路径等私有内容；不得公开上传备份和 validation 目录。
- 第一版保守保护 `@deepseek-ai/` 命名空间，修复窗口只允许改变第三方 bundle 的选择。

## 依据与实现选择

研究的官方源码提交：`46a7f68b0922371ce7144b668b90e377d8e799f4`。

- 官方 `packages/boot/app-boot/src/compatibility-preflight.ts` 可预检声明的 peer 要求；本机已安装的 alpha.2 未发现该接口，故未假装调用，也没有自行授权兼容豁免。
- 官方插件管理说明将 bundle 开关定义为修改 `dsh.profile.bundles`；Beta 在后端不可用时只做此最小文件适配，额外停用元数据保存在同一 JSON 中，以便一次原子提交。
- 官方 Desktop 的 fatal recovery 是禁用所有第三方 bundle 后重启；本 Beta 提供用户确认的单项停用，没有声称实现全面故障隔离。

源码仍为 C# / WinForms。构建脚本支持 prerelease 版本号与独立数值文件版本。
