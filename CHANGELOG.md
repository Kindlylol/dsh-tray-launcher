# Changelog

## 1.0.2 - 2026-08-28

- 将服务健康判断从“PID 存在且 3080 端口可连接”升级为实际调用 DSH `host.describe` API。
- 托盘菜单每次打开时重新检查状态；进程存在但 API 不可用时明确显示异常。
- 启动时发现受管 DSH 进程存活但 API 异常，会安全停止该进程后重新启动。
- 启动和重启流程等待 API 真正就绪，避免静态页面可访问但后台尚未可用时提前报成功。

## 1.0.1 - 2026-08-28

- 将软件、托盘和新建快捷方式的默认图标改为 DeepSeek Harness 官方黑鲸鱼图形。
- 针对 16 至 256 像素逐级生成并检查图标尺寸、留白和视觉重心。
- 在 EXE 内保留旧蓝鲸鱼图标，可从 Windows 快捷方式“更改图标”中选择。
- 同时检查 npm 与 Git 标签；只对 npm 中实际可安装的版本提供一键更新。
- Windows 启动等待改为异步流程，避免托盘界面被 40 秒轮询阻塞。
- 日志和服务状态迁移到 `%LOCALAPPDATA%\DSH Tray Launcher`，更适合便携目录和只读目录场景。

## 1.0.0 - 2026-08-22

- 首个正式作品版本
- 支持托盘启动、停止、重启和状态显示
- 增加受管 PID 与进程启动时间校验，避免误停无关进程
- 增加版本检测、更新失败恢复和单实例运行
- 使用官方蓝色鲸鱼图形重新制作多尺寸 Windows 图标
- 提供 Windows x64 自包含便携版
