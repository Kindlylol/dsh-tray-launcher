# Changelog

## 1.0.6 - 2026-09-20

### 同版本重新打包

- 提供压缩便携版（内置 .NET）与轻量版（需要 .NET 10 Desktop Runtime x64），功能一致。
- 轻量版附微软运行时下载快捷链接及版本说明；使用 .NET 原生缺失运行时提示。
- 原 v1.0.6 附件由上述两版替换，校验值更新；不修改 DSH 数据和业务逻辑。
- 两版构建和自动检查已验证；未在无 .NET 的干净 Windows 上完成下载弹窗及按钮跳转验证。

- 冷启动继承上次保存的核心/插件模式；旧配置没有模式记录时仍以核心启动。
- 本体更新先通过核心验收，再自动尝试插件模式；插件失败退回核心并记住核心模式，不回退已验收的新本体。
- npm deprecated 是上游依赖弃用提示；是否安装成功仍以退出码及核心验收为准。

## 1.0.5 - 2026-09-16

- 检测更新同时读取 npm latest、alpha，显示两个渠道的版本和可用状态，由用户选择更新。修复 latest 未变动时遗漏 Alpha 新版的问题。
- 不预选 Alpha；相同版本、旧版本、无效或未实际发布的版本不可更新。网络/数据错误单独报告，单渠道缺失不影响另一个渠道。
- 移除不能用于 npm 安装的 Git 标签检测，避免它阻塞更新检查。
- 右键菜单按状态、服务、运行模式、更新、诊断、桌面设置和退出分组，增加分隔线。
- 保留核心优先验收、独立运行时安装、插件失败回退和日志脱敏机制。
- 建议流程仍为：切换核心模式 → 检测并选择 latest/alpha 更新 → 核心正常启动 → 尝试插件模式。

## 1.0.4 - 2026-09-10

**建议更新流程：切换到核心模式 → 检测更新 → 等待更新完成且核心正常启动 → 尝试切换到插件模式。** 插件模式失败时继续使用自动恢复的核心模式，查看“插件诊断与修复命令”，修复后再试。

- Windows 本体优先：默认核心 profile；保留原 home 的会话、设置和凭据，插件组合留在原 web profile。
- 适配新版启动令牌换 Cookie，以及 `settings/describe` RPC；旧版保留 `host.describe`，请求 ID 必须匹配。
- 新运行时安装在独立目录，核心验收成功后才切换；安装或启动失败保留旧安装，并报告恢复是否成功。
- 插件模式验收失败后自动回到核心，提供脱敏诊断和手动修复命令，不因插件失败撤销本体更新。
- 修复 npm 独立安装依赖提升导致的协议识别错误；启动日志绑定当前进程，避免旧进程输出污染新认证。
- 增加核心隔离、RPC、插件失败回退及实际安装事务测试。

## 1.0.2 - 2026-08-28

- 将服务健康判断从“PID 存在且 3080 端口可连接”升级为实际调用 DSH `host.describe` API。
- 托盘菜单每次打开时重新检查状态；进程存在但 API 不可用时明确显示异常。
- 启动时发现受管 DSH 进程存活但 API 异常，会安全停止该进程后重新启动。
- 启动和重启流程等待 API 真正就绪，避免静态页面可访问但后台尚未可用时提前报成功。

## 1.0.1 - 2026-08-28

- 将软件、托盘和新建快捷方式的默认图标改为 DeepSeek Harness 官方黑鲸鱼图形。
- 针对 16 至 256 像素逐级生成并检查图标尺寸、留白和视觉重心。
- 在 EXE 内保留旧蓝鲸鱼图标，可从 Windows 快捷方式“更改图标”中选择。
- 同时检查 npm 与 Git 标签；只对 npm 中实际可安装的版本提供一键更新。
- Windows 启动等待改为异步流程，避免托盘界面被 40 秒轮询阻塞。
- 日志和服务状态迁移到 `%LOCALAPPDATA%\DSH Tray Launcher`，更适合便携目录和只读目录场景。

## 1.0.0 - 2026-08-22

- 首个正式作品版本
- 支持托盘启动、停止、重启和状态显示
- 增加受管 PID 与进程启动时间校验，避免误停无关进程
- 增加版本检测、更新失败恢复和单实例运行
- 使用官方蓝色鲸鱼图形重新制作多尺寸 Windows 图标
- 提供 Windows x64 自包含便携版
